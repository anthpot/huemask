# Python
__pycache__/
*.pyc
.pytest_cache/
venv/
.venv/

# 数据集（体积大，不入库）/ datasets (too large for the repo)
images/
masks/
masks_corrected/
1_图像/
2_掩膜/
3_修正掩膜/

# 运行期生成的类别配置(首次运行自动生成默认值)
# class config generated at runtime (defaults written on first run)
class_config.json
